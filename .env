# Dev local : laisser vide pour que Vite proxy /api → http://localhost:8080 (évite CORS et 403 prod)
VITE_API_BASE_URL=

# Production (décommenter seulement si le backend prod est à jour et autorise votre origine)
# VITE_API_BASE_URL=https://snjobconnect.com

# Backend direct sans proxy Vite
# VITE_API_BASE_URL=http://localhost:8080
